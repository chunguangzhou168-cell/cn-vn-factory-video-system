# 视频自动剪视频系统文档（小白操作版）

这是一个不需要安装开发工具即可运行的极简前端原型，包含：主控台、IP 定位、爆款拆解库、灵感库、选题库、文案库、视频执行库（素材、配音、自动剪辑入口）、数据分析库和复盘库。

## 第 1 步：打开系统

1. 找到项目文件夹中的 `index.html`。
2. 双击它，即可在浏览器打开系统。
3. 如果浏览器提示不能保存数据，请使用下面的本地服务器方式：在项目文件夹打开终端，输入：

   ```bash
   python3 -m http.server 8000
   ```

4. 浏览器访问 `http://localhost:8000`。

## 第 2 步：日常操作

1. 左侧点击功能模块切换页面。
2. 在「灵感库」「选题库」「文案库」右上角点击“新增”，输入内容后点“保存修改”。
3. 在主控台点击“创建新视频”，选择东莞或平阳工厂，任务会出现在“今日视频生产”。
4. 视频执行库预留了 **素材库管理、AI 文案编辑、语音配音、自动剪辑** 四个可替换入口；当前按钮会提示入口位置，后续可连接您的剪辑/配音工具。
5. 页面内的新增内容自动保存在当前浏览器；换浏览器或清理浏览器数据后需要重新录入。

## 第 3 步：小白可直接修改的位置

### 改页面文字、栏目名称和演示数据

用记事本或 VS Code 打开 `app.js`：

- 顶部的 `modules`：修改左侧栏目名称、图标和小标题。
- 顶部的 `defaults`：修改东莞/越南工厂信息、视频任务、灵感、选题和文案的初始演示内容。
- 搜索 `新手可修改区`：该区域有最重要的改法说明。

### 改颜色、间距和手机样式

用记事本或 VS Code 打开 `styles.css`：

- 最顶部 `:root` 中的 `--purple` 是主色；把 `#7165e8` 换成您喜欢的颜色即可。
- 搜索 `.sidebar` 可改左侧栏宽度和深色背景。
- 文件底部 `@media` 是手机/平板显示规则，一般不用修改。

### 接入真实功能（后续开发用）

在 `app.js` 搜索以下文字，即可找到对应按钮：

- `素材上传入口已预留`
- `开始配音`
- `开始剪辑`
- `已导出本周数据报表`

将这些按钮的提示功能替换为实际平台链接、接口或上传组件，即可逐步把原型升级为正式系统。

## 文件夹结构

```text
cn-vn-factory-video-system/
├── index.html                    # 页面骨架，不熟悉代码时不要大幅删除
├── styles.css                    # 所有视觉样式、颜色和响应式布局
├── app.js                        # 功能、菜单、演示数据和可视化编辑功能
└── 视频自动剪视频系统文档.md       # 本操作说明
```

## 以后如何删减模块

打开 `app.js`，找到顶部 `const modules = [...]`。每一行代表一个左侧模块；删除整行即可隐藏该模块。建议删除前复制一份该行到文档中备用。
